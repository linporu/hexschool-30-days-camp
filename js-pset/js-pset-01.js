const hello = () => {
  return "Hello world!";
};

const sayHi = (name) => {
  return `${name}, Hello World!`;
};

const add = (num1) => {
  return num1 + num1;
};

const addTwo = (num1, num2) => {
  return num1 + num2;
};

const addNumberAndString = (num1, num2, num3) => {
  return `第一加第二個參數加總為 ${num1 + num2}，第三個參數為 ${num3}`;
};
